/**
  Tracks file metadata
 */
CREATE TABLE FILE_INFO(
  ID			  INT not null,
  ENTRY_NAME       VARCHAR2(2000),
  FILE_DESCRIPTION VARCHAR2(2000),
  FILE_FULL_PATH  VARCHAR2(2000),  
  UPLOAD_USER     VARCHAR2(2000)
);